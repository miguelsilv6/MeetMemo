services:
  postgres:
    image: postgres:16-alpine
    container_name: meetmemo-postgres
    environment:
      - POSTGRES_DB=meetmemo
      - POSTGRES_USER=meetmemo
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/migrations:/docker-entrypoint-initdb.d
    networks:
      - meetmemo-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U meetmemo"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  meetmemo-backend:
    image: ghcr.io/notyusheng/meetmemo-backend:${IMAGE_TAG:-latest}
    container_name: meetmemo-backend
    volumes:
      - audiofiles:/app/audiofiles
      - logs:/app/logs
      - transcripts:/app/transcripts
      - summary:/app/summary
      - translations:/app/translations
      - exports:/app/exports
      - whisper_cache:/root/.cache/whisper
      - huggingface_cache:/root/.cache/huggingface
      - torch_cache:/root/.cache/torch
    runtime: nvidia
    environment:
      - HF_TOKEN=${HF_TOKEN:-CHANGEME}
      - LLM_API_URL=${LLM_API_URL:-CHANGEME}
      - LLM_MODEL_NAME=${LLM_MODEL_NAME:-CHANGEME}
      # Optional LLM settings, passed through only when set (bare form) so the
      # application defaults apply otherwise.
      - LLM_API_KEY
      - LLM_TIMEOUT
      - LLM_JSON_MODE
      - TIMEZONE_OFFSET=${TIMEZONE_OFFSET:-+8}
      # Hardware profile + ML overrides. Passed through only when set in the
      # environment (bare form), so an unset value lets HARDWARE_PROFILE's
      # auto-detection/defaults take effect instead of a hardcoded override.
      - HARDWARE_PROFILE
      - WHISPER_MODEL_NAME
      - COMPUTE_TYPE
      - PYANNOTE_MODEL_NAME
      - DEVICE
      # Admin panel. ADMIN_PASSWORD only bootstraps the account on first
      # start; afterwards the password is managed from the panel.
      - ADMIN_USERNAME
      - ADMIN_PASSWORD
      - ADMIN_SESSION_HOURS
      - DATABASE_URL=postgresql://meetmemo:${POSTGRES_PASSWORD:-changeme}@postgres:5432/meetmemo
      - PYTHONUNBUFFERED=1
      - NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES:-all}
      # Required for PyAnnote models - newer PyTorch defaults to weights_only=True for security,
      # but PyAnnote's pickled models require full unpickling. Only use with trusted model sources.
      - TORCH_LOAD_WEIGHTS_ONLY=0
    networks:
      - meetmemo-network
    depends_on:
      postgres:
        condition: service_healthy

  meetmemo-frontend:
    image: ghcr.io/notyusheng/meetmemo-frontend:${IMAGE_TAG:-latest}
    container_name: meetmemo-frontend
    # No external ports - accessed via nginx
    depends_on:
      - meetmemo-backend
    networks:
      - meetmemo-network

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: meetmemo-nginx
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
    depends_on:
      - meetmemo-frontend
      - meetmemo-backend
    networks:
      - meetmemo-network
    restart: unless-stopped

volumes:
  postgres_data:
  audiofiles:
  logs:
  transcripts:
  whisper_cache:
  huggingface_cache:
  torch_cache:
  summary:
  translations:
  exports:

networks:
  meetmemo-network:
    name: meetmemo-network
    driver: bridge